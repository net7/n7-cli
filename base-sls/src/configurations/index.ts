//CONFIGURATION
import resourcesConfig from "./resources/index";
import homeConfig from './home_config';
import searchConfig from './search_config';
import footerConfig from './footer_config';

export default {
    resources: resourcesConfig,
    home: homeConfig,
    footer: footerConfig,
    search: searchConfig
}